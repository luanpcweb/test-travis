language: php
php:
  - nightly
  - 5.6
  - 7.0
  - 7.1
  - 7.2

before_install:
  - composer self-update

install:
  - travis_retry composer install --dev --no-interaction --prefer-source

before_script:
  - mkdir -p build/logs

script:
  - vendor/bin/phpunit

sudo: false
